export default function Footer() {
  return (
    <footer className="border-t border-zinc-200 px-4 py-6 text-center dark:border-white/10">
      <p className="text-xs text-zinc-400 dark:text-zinc-500">
        We collect anonymous usage analytics to help improve FPL Assistant — no accounts, no
        personal data collected automatically.
      </p>
    </footer>
  );
}
